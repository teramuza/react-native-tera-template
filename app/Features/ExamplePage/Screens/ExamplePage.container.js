import { connect } from 'react-redux';
import ExampleScreen from './ExamplePage.screen';

const mapStateToProps = state => ({});

const mapDispatchToProps = dispatch => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(ExampleScreen);
